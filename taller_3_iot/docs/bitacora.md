# Bitácora — UNAB-Ambiental, Laboratorio 3

**Estudiante:** José Alejandro Téllez Prada
**Curso:** IoT + Cloud + Sistemas Distribuidos (UNAB)
**Plantilla:** consola-unab-ambiental

---

## [2026-09-11 12:44] Verificación de estado heredado del Lab 2 (pre-Etapa 1)
- **Qué se hizo:** se chequeó el estado REAL de los componentes, sin asumir nada: (a) VM del nodo Python — `Test-NetConnection` al host de la VM: TCP/22=False, TCP/5000=False, ping=False → VM **apagada/inaccesible** (dos días después del re-arranque verificado el 2026-09-09); credenciales SSH OK en archivo local fuera del repo, pero sin VM encendida no hay acceso; el nodo desplegado y el servicio de mail quedan en pausa hasta que el usuario la re-encienda desde el portal. (b) Wokwi — la simulación es una GUI de VS Code; no está corriendo en este momento (`wokwi/serial.log` = 0 bytes). (c) Laptop local — salida directa verificada con `Test-NetConnection global.azure-devices-provisioning.net -Port 8883` → TcpTestSucceeded=**True**.
- **Por qué:** requisito de la misión: documentar el estado actual antes de iniciar la Etapa 1, y decidir dónde se ejecutarán las etapas siguientes.
- **Resultado / evidencia:** **Decisión D1 (Plan B):** las Etapas 2–3 se ejecutan desde la laptop local (única vía viva ahora), manteniendo `mqtt_explicito.py` compatible con re-ejecución en la VM (subida con 1 comando vía `tools/vm_push.py` cuando la VM vuelva). Se verificará al cierre que nada del Lab 2 (SDK del nodo, Rule de email T>27, proyecto Wokwi) resultó dañado — su funcionamiento previo está documentado en la bitácora del Lab 2 y fue verificado in-situ el 2026-09-09.

## [2026-09-11 12:50] Apertura del Laboratorio 3
- **Qué se hizo:** se creó la estructura del laboratorio: carpeta `taller_3_iot/` con `docs/` (esta bitácora), `evidencias/` (logs, tablas y capturas; con `.gitkeep`), `README.md` (objetivo y las 4 etapas), `.gitignore` (excluye `.env`, `__pycache__/`, `*.pyc`, `.venv/`) y `.env.example` (placeholders: hostname del hub, device ID, primary key, intervalo, salón — sin valores reales). Además se añadió al FINAL de la bitácora del Lab 2 un addendum de trazabilidad sobre el servicio de correo (sin reescribir entradas previas del Lab 2).
- **Por qué:** formalizar el laboratorio que "abre la caja negra" del MQTT que el SDK de Azure IoT ocultó en el Lab 2, sin tocar el código ni la configuración del Lab 2.
- **Resultado / evidencia:** esqueleto creado y verificado en disco. Próximo paso: Etapa 1 — documentar el MQTT oculto (hostname completo del hub asignado por DPS, puerto 8883/TLS, payload JSON real, credenciales MQTT).

## [2026-09-11 13:07] Etapa 1.1 — MQTT oculto del nodo Python (SDK)
- **Qué se hizo:** se ejecutó desde la laptop (única vía viva, verificada a las 12:56) un script desechable fuera del repo (`%TEMP%\opencode\`, ya borrado) con `azure-iot-device` 2.14.0 que hace **SOLO** el registro DPS (`ProvisioningDeviceClient.create_from_symmetric_key(...)` + `register()`) con las credenciales de `taller_2_iot/.env` leídas solo en memoria, nunca impresas. **NO** se hizo `connect()` al hub ni se envió telemetría. Salida completa (redactada, sin secretos) en `evidencias/etapa1-dps-python-vm.log`. Se inspeccionó además el código fuente del paquete instalado para destapar el transporte que el SDK oculta. *(Entrada consolidada a las 13:12 a partir de las mediciones de tres sesiones concurrentes — ver nota de integridad al final.)*
- **Por qué:** destapar la "caja negra" con datos reales (no asumidos): capturar el hostname COMPLETO del hub que el SDK obtuvo por DPS y documentar qué MQTT hay por debajo, con cero riesgo para el solution del Lab 2 — el registro es la misma operación **idempotente** que el nodo ya hacía al arrancar (con subestado `initialAssignment`, registrar de más devuelve la asignación existente, no re-asigna). Verificación previa de colisión: VM del nodo apagada (TCP/22 = False, 12:55) → el device no estaba en uso.
- **Resultado / evidencia:**
  - **Hub asignado REAL (capturado hoy):** `registration_id`/device = `23z8rpgm6s4`, `ID_SCOPE` = `0ne0128547D`, `status: assigned`, `sub_status: initialAssignment`, **`assigned_hub` = `iotc-47a415d3-3f15-4cf1-918c-4be1461915f3.azure-devices.net`** — hostname completo que la bitácora del Lab 2 tenía truncado. Fechas de la asignación DPS: `created_date_time` = 2026-09-11T18:07:00.7695675Z, `last_update_date_time` = 2026-09-11T18:07:00.959762Z. `INTERVALO_SEGUNDOS` = **10** s (valor no secreto del `.env`; coincide con la telemetría cada ~10 s de la bitácora Lab 2, p. ej. 09-02 20:55). `ID_SALON=A-301` **NO** es telemetría: es *reported property* PnP del gemelo, viaja por otro tema.
  - **Transporte oculto:** MQTT **3.1.1** sobre TLS **puerto 8883** — `azure/iot/device/common/mqtt_transport.py`: l. 151 `mqtt.Client(..., protocol=mqtt.MQTTv311)` (el "motor" es literalmente **paho-mqtt**, `clean_session=False`), l. 395–397 `"Connect using port 8883 (TCP)"`, l. 324 `ssl.SSLContext(protocol=ssl.PROTOCOL_TLSv1_2)`; alternativa WebSocket por **puerto 443** con path `/$iothub/websocket` (l. 148).
  - **Credenciales MQTT que el SDK arma solo:** username `{hostname}/{device_id}/?api-version=...&DeviceClientType=...` (`iothub/pipeline/pipeline_stages_iothub_mqtt.py` l. 73–77: `{hostname}/{client_id}/?{query_params}`, client_id = device_id); password = token SAS `SharedAccessSignature sr={resource}&sig={signature}&se={expiry}` (`common/auth/sastoken.py` l. 30–32) con `sig` = **HMAC-SHA256**(base64-decode(key), quote(uri)+"\n"+expiry) → base64 → quote (`common/auth/signing_mechanism.py` l. 44–61). El registro autenticó con solo la clave simétrica → prueba empírica de autenticación SAS por key compartida (sin certificado de cliente ni usuario/contraseña).
  - **`api-version` REAL del SDK 2.14.0 = `2019-10-01`** (`azure/iot/device/constant.py:12`; `PROVISIONING_API_VERSION` = `2019-03-31`, `DIGITAL_TWIN_API_VERSION` = `2020-09-30`, l. 13–21). La documentación de IoT Hub se cita a menudo con `2021-04-12`: el cliente explícito de la Etapa 2 usará `2021-04-12` y la diferencia quedará documentada en la comparación de la Etapa 4.
  - **Topics que el SDK usa por debajo de cada función de `python-vm-01.py`:** telemetría `devices/23z8rpgm6s4/messages/events/` (`iothub/pipeline/mqtt_topic_iothub.py` l. 97) ← `send_message(json.dumps(lectura))` (l. 376/437 → PUBLISH, con propiedades URL-encoded en el tema); métodos `$iothub/methods/POST/#` ← `receive_method_request` (l. ~360 → SUBSCRIBE, comandos); gemelo `$iothub/twin/PATCH/properties/desired/#` y `$iothub/twin/res/#` ← `receive_twin_desired_properties_patch` (l. ~396 → SUBSCRIBE); `patch_twin_reported_properties` (l. 441) → PUBLISH a `$iothub/twin/PATCH/properties/reported/?$rid=`.
  - **QoS:** el pipeline publica telemetría **sin pasar `qos`** → default **QoS 1** (`mqtt_transport.py` l. 522 `def publish(self, topic, payload, qos=1, ...)`, llamado en `common/pipeline/pipeline_stages_mqtt.py` l. 278); las suscripciones del SDK también usan `qos=1` (`mqtt_transport.py` l. 468). Afirmación: el SDK envía telemetría al hub con QoS 1.
  - **Redacción verificada:** `grep -oE "[A-Za-z0-9+/]{44}"` sobre las salidas → **0 coincidencias** (ninguna symmetric key ni SAS real en logs ni en esta bitácora). Auditoría adicional de CONTENCIÓN exacta de los valores reales (sin imprimirlos) sobre todo `taller_3_iot/**` → **0 apariciones de secretos** (13:11); nota: las 3 menciones de IP/usuario de la VM en la bitácora del Lab 2 son PRE-EXISTENTES (recuento idéntico en `git show HEAD`; líneas del 09-02/09-03 escritas por el estudiante).
  - **Réplicas cruzadas:** tres sesiones ejecutaron este registro de forma independiente (13:02–13:11) y obtuvieron el MISMO hub/status/subestado/intervalo → la ciencia está triplemente replicada y la idempotencia DPS quedó demostrada empíricamente.
- **Nota de entorno (honestidad):** en el venv local quedó instalado `paho-mqtt 2.1.0` (por trabajo de la Etapa 2), y `azure-iot-device 2.14.0` declara `paho-mqtt<2.0.0`; la ejecución de hoy funcionó correctamente (el SDK usa kwargs compatibles con el shim legacy de paho 2.x), pero es un conflicto de metadatos a vigilar.

## [2026-09-11 13:07] Etapa 1.2 — MQTT del ESP32 (Wokwi)
- **Qué se hizo:** segundo script desechable (mismo patrón register-only, ya borrado de `%TEMP%`) para `esp32-wokwi-01`, parseando `DPS_ID_SCOPE`, `IOT_CONFIG_DEVICE_ID` e `IOT_CONFIG_DEVICE_KEY` de `taller_2_iot/wokwi/iot_configs.h` **dentro del proceso** (la KEY pasó directo al objeto de seguridad en memoria; jamás se imprimió ni se copió a ningún archivo). Salida redactada en `evidencias/etapa1-dps-wokwi.log`. *(Consolidada 13:12 a partir de las mediciones de tres sesiones concurrentes.)*
- **Por qué:** comprobar con evidencia en vivo (sin asumir) a qué hub lo asigna DPS hoy, verificar cruzadamente el hostname truncado que quedó documentado en la bitácora del Lab 2, y comprobar empíricamente si los dos caminos del Lab 2 (SDK Python y SDK Azure C en Wokwi) terminan en el mismo broker.
- **Resultado / evidencia:**
  - **Hub asignado REAL:** `id_scope` = `0ne0128547D`, `registration_id` = `esp32-wokwi-01`, `status: assigned`, `sub_status: initialAssignment`, `assigned_hub` = **`iotc-47a415d3-3f15-4cf1-918c-4be1461915f3.azure-devices.net`** (`last_update` 2026-09-11T18:07:12.4093988Z) — **el MISMO hub que el device Python** (ambos devices de la misma app de IoT Central comparten el hub subyacente) → en la Etapa 2 basta un broker destino para el cliente explícito.
  - **Verificación cruzada:** coincide con el `iotc-47a415d3-...azure-devices.net` truncado de la bitácora Lab 2 (línea 192, entrada [2026-09-03 22:15]): mismo prefijo y formato → hostname completo confirmado ✔.
  - **Flujo DPS del Azure SDK for C (evidencia serial Lab 2, misma línea 192):** `PUT topics/iotdps-register/` → `GET topics/iotdps-get-operationstatus/` → asignación → `MQTT CONNECT` al hub con password SAS + suscripciones PnP (`$iothub/methods/POST/#`, `$iothub/twin/res/#`, desired properties).
  - **Parámetros del firmware:** `TELEMETRY_FREQUENCY_IN_SECONDS` = **10** s (`iot_configs.h:27`), `MODEL_ID` = `dtmi:unabAmbientalJose:consolaUnabAmbiental_1pu;1`, vida del token `MQTT_PASSWORD_LIFETIME_IN_MINUTES` = 60 (`iot_configs.h`), user-agent `c/1.4.0(ard;esp32)` visible en el CONNECT (Lab 2). El SDK C (azure-iot-sdks for Embedded C, `az_iot_hub_client` — MQTT crudo también al 8883) publica en el topic PnP `devices/{id}/messages/events/` con `api-version=2020-09-30` y `$.if=` para componentes.
  - **Pendiente del usuario:** la ejecución en vivo del sketch requiere Wokwi (GUI de VS Code) encendido — la captura del CONNECT real queda como evidencia pendiente del usuario; lo documentado aquí proviene del registro DPS ejecutado hoy + serial del Lab 2.

## [2026-09-11 13:08] Etapa 1.3 — Payload JSON y afirmación de transporte
- **Qué se hizo:** reproducir la lógica exacta de `generar_lectura()` de `python-vm-01.py` (líneas 329–340, mismo código, valores de ejemplo por `random`) y ejecutarla para capturar el payload real en bytes; revisar la construcción del payload en el firmware (`Azure_IoT_PnP_Template.cpp`); cerrar la afirmación de transporte con evidencia capturada hoy (`Test-NetConnection` al hub asignado, 13:08). *(Consolidada 13:12 a partir de las mediciones de tres sesiones concurrentes.)*
- **Por qué:** el payload es lo único que viaja "visible" en el PUBLISH; documentarlo cierra la caja negra junto con la afirmación de transporte, y las Etapas 2–3 replicarán manualmente (paho) este mismo payload/topics — hay que partir de la forma real observada, no de suposiciones.
- **Resultado / evidencia:**
  - **Payload Python:** JSON plano `{"Temperature": <float 16–32>, "Humidity": <float 35–70>, "Iluminance": <float 100–800>}` (1 decimal, rangos físicos DTDL respetados), publicado por `send_message(json.dumps(lectura))` como cuerpo del PUBLISH a `devices/23z8rpgm6s4/messages/events/`. Ejemplos generados con la MISMA lógica random-walk (estado inicial 24.0/50.0/400.0), de **ejecuciones distintas** — no valores inventados, conservados todos: (a) ejecución de las 13:07 → `{"Temperature": 24.5, "Humidity": 50.5, "Iluminance": 415.2}`; (b) ejecución de las 13:07 con medición de bytes → `{"Temperature": 23.5, "Humidity": 49.7, "Iluminance": 394.4}` = **60 bytes UTF-8**; (c) ejemplo del formato que envía `send_telemetry_message(Data=...)` → `{"Temperature": 25.4, "Humidity": 52.1, "Iluminance": 340.0}`. Ejemplo real del Lab 2: lectura forzada `Temperature: 28.4` (bitácora Lab 2 l. 118).
  - **Payload ESP32 (fuente código):** mismo formato `{Temperature, Humidity, Iluminance}` — mismas 3 propiedades vía `#define TELEMETRY_PROP_NAME_{TEMPERATURE,HUMIDITY,ILUMINANCE}` (`Azure_IoT_PnP_Template.cpp` l. 26–28) serializadas con `az_json_writer` (l. 360–370) en `generate_telemetry_payload()` (l. 341+), tras `read_sensors()` (DHT22 + LDR); publicado a `devices/esp32-wokwi-01/messages/events/` cada 10 s (bitácora Lab 2 [2026-09-03 22:15]). Lecturas reales del serial Lab 2 (09-02): T≈27–30 °C, H≈55–57 %, Lux≈100–123.
  - **Modelo:** los IDs `Temperature`/`Humidity`/`Iluminance` coinciden EXACTAMENTE con la plantilla DTDL `consola-unab-ambiental` (`dtmi:unabAmbientalJose:consolaUnabAmbiental_1pu;1`). La confirmación visual en *Data explorer* del portal requiere sesión web del usuario → **queda como captura pendiente del usuario**, no se inventa → evidencia `evidencias/15-data-explorer-telemetria.png`.
  - **AFIRMACIÓN FINAL (Etapa 1):** el transporte de ambos nodos es **MQTT 3.1.1 sobre TLS hacia Azure IoT Central (hub `iotc-47a415d3-3f15-4cf1-918c-4be1461915f3.azure-devices.net`), puerto 8883, autenticación SAS (HMAC-SHA256), QoS 1 por defecto del SDK**. Tres piezas de evidencia: (1) el hostname `*.azure-devices.net` es el endpoint del IoT Hub subyacente de la app de IoT Central y fue devuelto POR DPS hoy mismo para ambos devices (`evidencias/etapa1-dps-python-vm.log` y `evidencias/etapa1-dps-wokwi.log`, más re-registro independiente de las 13:11 que devolvió la misma asignación); (2) 8883 es el puerto MQTT-TLS: constante del SDK (`mqtt_transport.py` l. 395–397) + `Test-NetConnection iotc-47a415d3-…azure-devices.net -Port 8883` → **True**, ejecutado hoy 13:08 — el puerto MQTT-TLS del hub asignado está vivo y alcanzable; (3) SAS: ambos registros DPS de hoy autenticaron con éxito usando únicamente la symmetric key → el SDK firma `sr+se` con **HMAC-SHA256** (`signing_mechanism.py`) y arma `SharedAccessSignature sr={resource}&sig={signature}&se={expiry}` (`sastoken.py`).

## [2026-09-11 13:11] Nota de integridad — duplicación de entradas Etapa 1 por sesiones concurrentes
- **Qué se detectó:** tres sesiones Worker ejecutaron T2.1 en paralelo y cada una añadió su propio juego de entradas "Etapa 1" (bloques fechados 13:02–13:08). La bitácora quedó con contenido triplicado.
- **Por qué importa:** las reglas prohíben reescribir entradas históricas, y ningún Worker debe borrar trabajo de otra sesión; se deja constancia honesta en vez de ediciones destructivas.
- **Resultado / evidencia:** las tres ejecuciones midieron de forma independiente lo MISMO: ambos devices (`23z8rpgm6s4`, `esp32-wokwi-01`) `assigned` por DPS al hub `iotc-47a415d3-3f15-4cf1-918c-4be1461915f3.azure-devices.net`, MQTT 3.1.1/TLS 8883, SAS HMAC-SHA256, QoS 1 por defecto, payload `{Temperature, Humidity, Iluminance}`, intervalo 10 s → la ciencia está triplemente replicada; queda como acción para el Revisor consolidar los bloques duplicados (fusión no-destructiva o marcada como nota al margen).
- **RESUELTA 13:12:** consolidación ejecutada (fusión no-destructiva) por el Worker asignado por el Commander: los tres juegos de entradas Etapa 1 (bloques duplicados 13:02–13:08 + la re-verificación cruzada de las 13:11) se fusionaron en exactamente TRES entradas limpias (1.1, 1.2, 1.3) conservando TODOS los hechos únicos y las citas de líneas de código de cada versión. Donde dos ejecuciones produjeron valores de ejemplo distintos (payloads 24.5/50.5/415.2 vs 23.5/49.7/394.4 vs 25.4/52.1/340.0) se conservan todos, etiquetados como ejemplos de ejecuciones distintas. Las tres mediciones independientes coinciden y quedan documentadas como réplicas cruzadas dentro de las entradas fusionadas; las entradas previas a la Etapa 1 (12:44 y 12:50) no se tocaron.

## [2026-09-11 13:23] Etapa 2 — Cliente MQTT explícito funcionando (sin SDK)
- **Qué se hizo:** implementado `mqtt_explicito.py` (270 líneas, paho-mqtt 2.1.0, sin `azure-iot-device`): SAS HMAC-SHA256 construido a mano (`sr={hub}/devices/{id}&sig&se`, solo en memoria), TLS 8883 con verificación de certificado, username `{hub}/{id}/?api-version=2021-04-12`, PUBLISH telemetría `{Temperature,Humidity,Iluminance}` a `devices/{id}/messages/events/`, suscripciones twin/desired + twin/res + methods + C2D, GET de gemela al conectar, reported `ID_salon` por twin (no telemetría), ack de métodos (`$iothub/methods/res/200/?$rid=`), backoff de reconexión 1–10 s, `--qos/--duration/--cut-at/--log`. Nota de proceso: dos sesiones (Commander y el Worker en curso) escribieron versiones del mismo script — se consolidó QUEDÁNDOSE con la del Worker (URI de recurso SAS correcta `{hub}/devices/{id}`), verificada por compilación y ejecución real. El primer intento con mi variante reveló el bug pedagógico de paho 2.x: `username_hint/password_hint` ya no existen → CONNACK `Not authorized`; el fix (`username_pw_set`) confirma empíricamente que user+password SAS son LA autenticación MQTT.
- **Por qué:** núcleo de la misión — demostrar que todo lo que el SDK hacía "mágico" es MQTT estándar construible a mano.
- **Resultado / evidencia:** smoke real 45 s QoS1 desde la laptop (device `23z8rpgm6s4`, VM caída = sin colisión, decisión D2): `exit=0`, CONNACK Success, 5 PUBLISH → 5 PUBACK (RTT avg 577.7 ms, min 172.8, max 1165.4), 4 SUBACK concedidos, gemela respondida (`$iothub/twin/res/#` y PATCH desired→reported con `$version`), payload 60 bytes idéntico en forma al del Lab 2. Logs: `evidencias/etapa2-smoke2.{log,jsonl}` (scan de contención de secretos = CLEAN, 0 patrones `sig=`). `py_compile` OK. Pendiente usuario: ver en portal que la telemetría del smoke aparece en Data explorer (captura `16-smoke-explícito.png`).

## [2026-09-11 19:47] Etapa 3 — Campaña de mediciones QoS 0/1/2 y corte (cliente explícito, Plan B laptop)
- **Qué:** cuatro campañas secuenciales contra el device `23z8rpgm6s4` con `mqtt_explicito.py` (paho, sin SDK), intervalo configurado 10 s, payload `{Temperature, Humidity, Iluminance}` = 60 bytes exactos por mensaje. Evidencias crudas (JSONL con SUMMARY autocontenidos): `evidencias/etapa4-qos1-clean.jsonl` (QoS1, 150 s), `etapa4-qos0-clean.jsonl` (QoS0, 150 s), `etapa4-qos2-handshake.jsonl` (QoS2 + `--debug-packets`, 60 s), `etapa4-corte-clean.jsonl` (QoS1 + `--cut-at 25`, re-ejecución limpia única; dos réplicas previas intercaladas se conservan en `etapa4-corte-COLLIDED.jsonl`). Corridas por el Worker W1 (sesión reanudada 19:25); tablas extraídas por el Revisor directamente de los SUMMARY (trazabilidad 1:1).
- **Por qué:** cumplir S4.1.1–S4.1.4 con números reales: comparar RTT y jitter por nivel de QoS, probar empíricamente QoS 2 contra IoT Central (decisión D4) y observar la reconexión del cliente explícito ante corte (comparativa con SDK Lab 2 y Wokwi).
- **Resultado — Tabla QoS 1 (Run A, 15/15 acks, 1 conexión):** RTT publish→PUBACK avg **183.1 ms** (mín 169.5, máx 267.6); gap observado entre publicaciones avg **10.024 s** (mín 10.016, máx 10.067 → jitter máx +67 ms sobre los 10 s configurados).

| seq | bytes | t_pub (UTC) | t_ack (UTC) | RTT ms |
|----:|------:|-------------|-------------|-------:|
| 1 | 60 | 00:22:05.474 | 00:22:05.741 | 267.6 |
| 2 | 60 | 00:22:15.499 | 00:22:15.669 | 169.5 |
| 3 | 60 | 00:22:25.516 | 00:22:25.692 | 176.0 |
| 4 | 60 | 00:22:35.536 | 00:22:35.716 | 180.7 |
| 5 | 60 | 00:22:45.560 | 00:22:45.736 | 176.4 |
| 6 | 60 | 00:22:55.592 | 00:22:55.773 | 180.7 |
| 7 | 60 | 00:23:05.620 | 00:23:05.796 | 176.1 |
| 8 | 60 | 00:23:15.636 | 00:23:15.807 | 171.0 |
| 9 | 60 | 00:23:25.656 | 00:23:25.836 | 179.3 |
| 10 | 60 | 00:23:35.723 | 00:23:35.902 | 179.3 |
| 11 | 60 | 00:23:45.741 | 00:23:45.914 | 172.7 |
| 12 | 60 | 00:23:55.759 | 00:23:55.940 | 180.7 |
| 13 | 60 | 00:24:05.778 | 00:24:05.957 | 179.6 |
| 14 | 60 | 00:24:15.797 | 00:24:15.980 | 182.4 |
| 15 | 60 | 00:24:25.813 | 00:24:25.987 | 173.9 |

- **Resultado — Tabla QoS 0 (Run B, 15/15, 1 conexión):** "RTT" avg **0.3 ms** (0.2–0.4) — NO es ida-y-vuelta al hub: en QoS 0 el callback de publicación se dispara al entregar al buffer local de paho, sin ack del servidor. Gap observado avg 10.023 s (mín 10.014, máx 10.047). Comparar 0.3 ms vs 183.1 ms sería un error de interpretación: QoS 1 mide red+hub; QoS 0 mide solo memcpy local.

| seq | bytes | t_pub (UTC) | t_ack local (UTC) | "RTT" ms |
|----:|------:|-------------|-------------------|---------:|
| 1 | 60 | 00:25:21.447 | 00:25:21.448 | 0.4 |
| 2 | 60 | 00:25:31.466 | 00:25:31.466 | 0.2 |
| 3 | 60 | 00:25:41.513 | 00:25:41.514 | 0.4 |
| 4 | 60 | 00:25:51.531 | 00:25:51.531 | 0.4 |
| 5 | 60 | 00:26:01.549 | 00:26:01.549 | 0.3 |
| 6 | 60 | 00:26:11.578 | 00:26:11.578 | 0.3 |
| 7 | 60 | 00:26:21.604 | 00:26:21.604 | 0.3 |
| 8 | 60 | 00:26:31.618 | 00:26:31.619 | 0.3 |
| 9 | 60 | 00:26:41.636 | 00:26:41.636 | 0.4 |
| 10 | 60 | 00:26:51.651 | 00:26:51.651 | 0.2 |
| 11 | 60 | 00:27:01.672 | 00:27:01.672 | 0.2 |
| 12 | 60 | 00:27:11.694 | 00:27:11.694 | 0.3 |
| 13 | 60 | 00:27:21.713 | 00:27:21.714 | 0.3 |
| 14 | 60 | 00:27:31.729 | 00:27:31.730 | 0.3 |
| 15 | 60 | 00:27:41.770 | 00:27:41.771 | 0.3 |

- **Resultado — QoS 2 (Run C, 6/6, 1 conexión, RTT avg 187.0 ms, gap avg 10.020 s): HALLAZGO (responde D4).** Con `--debug-packets` volcado en `etapa4-qos2-handshake.jsonl`: el cliente envía PUBLISH con flags QoS 2, pero el hub de IoT Central responde **PUBACK** (6/6), el paquete de QoS 1 — NUNCA aparece PUBREC/PUBREL/PUBCOMP en el alambre. paho reporta `qos_eff: 2` en el callback (vista cliente), pero la evidencia de red muestra que **Azure IoT Hub no honra QoS 2: lo degrada a semántica QoS 1**. No hay rechazo ni desconexión: la sesión queda funcional con ack único. RTT (187.0 ms) es estadísticamente igual al de QoS 1 (183.1 ms), coherente con que el servidor está haciendo PUBACK.

| seq | bytes | t_pub (UTC) | t_ack (UTC) | RTT ms | paquete recibido |
|----:|------:|-------------|-------------|-------:|------------------|
| 1 | 60 | 00:39:27.471 | 00:39:27.716 | 245.4 | PUBACK (no PUBREC) |
| 2 | 60 | 00:39:37.492 | 00:39:37.673 | 181.7 | PUBACK |
| 3 | 60 | 00:39:47.513 | 00:39:47.695 | 182.7 | PUBACK |
| 4 | 60 | 00:39:57.531 | 00:39:57.702 | 170.8 | PUBACK |
| 5 | 60 | 00:40:07.549 | 00:40:07.723 | 173.8 | PUBACK |
| 6 | 60 | 00:40:17.571 | 00:40:17.739 | 167.9 | PUBACK |

- **Resultado — Corte (Run D limpia, `--cut-at 25`, `etapa4-corte-clean.jsonl`, 9/9 acks, 2 conexiones, RTT avg 186.6 ms):** el socket se cierra a mano a los ~25 s (`CUT_SIMULADO`, equivalente a cable suelto; la regla de firewall de Windows requiere admin y no se usó — fallback D3 ejecutado). Secuencia observada: CONNACK n=1 → pubs 1–3 → `DISCONNECT rc="Unspecified error"` + `CUT_SIMULADO` (~00:50:49Z) → **CONNACK n=2 con backoff de paho** → pubs 4–9 todos con PUBACK → DISCONNECT normal al cierre. `session_present=True` en ambos CONNACK. **Ningún mensaje se perdió** (9 publicados, 9 acks). Dos réplicas previas del mismo experimento (4 y 5 conexiones, 9/9 acks cada una) quedaron preservadas en `etapa4-corte-COLLIDED.jsonl` como evidencia secundaria de replicación. Comparativa: el SDK de Lab 2 documentó reconexión equivalente (bitácora Lab 2 09-02 21:25: el hub reanudó sin pérdida tras corte de red de la VM); Wokwi/Arduino requiere `WiFi.reconnect()` + máquina de estados manual en el sketch (Etapa 1.2) — el cliente explícito con paho hereda la reconexión de la librería, sin código propio.
- **S4.1.4 — latencia end-to-end:** lo medible localmente quedó medido (publish→PUBACK: 169–268 ms; reloj del mensaje en t_pub UTC). La marca de recepción REAL en IoT Central (Data explorer) requiere sesión web del usuario → evidencia pendiente `15-data-explorer-telemetria.png`. NO se inventó ningún número de Central.
- **Notas de integridad:** (1) `etapa4-qos2-handshake-COLLIDED.*` quedó en CUARENTENA (dos escritores simultáneos detectados a las 19:37; no se usó para ninguna tabla; su re-ejecución limpia es la Run C documentada arriba — las "17 conexiones" del archivo contaminado eran artefacto de la colisión, no comportamiento de QoS 2). (2) `etapa4-qos1-clean.log` fue sobreescrito por un reintento fallido (132 bytes con error de ruta); la trazabilidad de la Run A se ancla a su `.jsonl` (15/15, íntegro). (3) Las dos primeras réplicas del corte se lanzaron con solapamiento (~4 s) y compartieron archivo con eventos intercalados; W1 las renombró a `etapa4-corte-COLLIDED.*` (19:48) y re-ejecutó una corrida limpia única — esa es la Run D oficial de arriba. Las réplicas contaminadas se conservan como evidencia secundaria (cada SUMMARY interno era 9/9, coherente con la limpia). (4) Cero secretos: ni SAS tokens ni keys en logs ni en esta entrada; hostname documentado desde Etapa 1.

## [2026-09-11 19:56] Etapa 4.1 — Comparativa final: SDK (Lab 2) vs MQTT explícito (Lab 3) vs Wokwi/Arduino
- **Qué:** cierre de la caja negra con tabla comparativa usando ÚNICAMENTE datos medidos en Lab 2 (bitácora 09-02) y Lab 3 (Etapa 3, corridas 19:22–19:52 de hoy).
- **Por qué:** la pregunta central del lab era si "abrir la caja negra" cambia algo práctico para UNAB-Ambiental; la respuesta debe estar anclada en evidencia, no en opinión.

| Criterio | SDK azure-iot-device (Lab 2, VM) | MQTT explícito paho (Lab 3) | Wokwi/Arduino (Lab 2, ESP32) |
|---|---|---|---|
| Facilidad de implementación | Alta: `IoTHubDeviceClient.create_from_connection_string` + `send_event`; 478 líneas propias, pero dependencia de un paquete de miles de líneas cuyo comportamiento no se ve | Media: 301 líneas totales, cero dependencias SDK (solo paho-mqtt); hay que armar SAS (HMAC-SHA256), username/password, topics y twin-ack a mano — conocimiento que el SDK ocultaba | Baja-media: 652+1492 líneas de plantilla PnP generada + `WiFi.reconnect` + máquina de estados `azure_iot_stop/start` escrita a mano |
| Control del protocolo | Nulo observable (QoS "por defecto 1" sin API directa; sin visibilidad de paquetes) | Total: QoS elegible por flag, volcado de paquetes byte-a-byte (`--debug-packets`), suscripciones twin/methods/C2D explícitas | Parcial vía API del SDK de C; sin visibilidad de paquetes en el sketch |
| Tamaño de payload | `{Temperature, Humidity, Iluminance}` = 60 B (Etapa 1.3) | Idéntico: 60 B en los 45 mensajes medidos hoy | Idéntico (misma plantilla DTDL) |
| Latencia percibida | "Baja" cualitativa en Lab 2 (sin medición) | **Medida: 169.5–267.6 ms publish→PUBACK (avg 183.1, n=15, QoS1)**; QoS0 "0.3 ms" es ack local, no red | No medible localmente (simulador); depende del internet de Wokwi |
| Reconexión | Automática del SDK (documentada 09-02 21:25, sin pérdida) | Automática de paho con backoff: corte a los 25 s → 1 reconnect → 9/9 acks, `session_present=True` (Run D) | Manual en código: máquina de estados + re-provisionamiento DPS al reconectar |
| Hallazgo único de Lab 3 | — | **IoT Hub degrada QoS 2 → PUBACK (QoS1) en el alambre** (6/6, cero PUBREC/PUBREL/PUBCOMP): el SDK de Lab 2 enviaba QoS1 porque QoS2 no está soportado | — |
| Utilidad UNAB-Ambiental | Suficiente para operar; insuficiente para diagnosticar | Ideal para depurar/diagnosticar (se ve el protocolo) y para entender qué se pierde al cambiar de plataforma | Único camino en hardware real ESP32; el más frágil ante cortes |

- **Conclusión accionable:** para el aula y el monitoreo diario, el SDK sigue siendo el camino correcto (menos código, reconexión probada); el cliente explícito se queda como **herramienta de diagnóstico** del proyecto: cuando Central no reciba datos, `mqtt_explicito.py --debug-packets` distingue en minutos si el problema es TLS, SAS, topic o QoS — algo imposible de observar desde el SDK. Y si algún día se migra a un broker propio (Mosquitto en el salón), el cliente explícito es reutilizable casi sin cambios, mientras que el código del SDK habría que reescribirlo.

## [2026-09-11 19:56] Etapa 4.2 — Verificación de integridad de Lab 2 y evidencias portal pendientes
- **Qué:** confirmación final de que Lab 3 no rompió Lab 2, y consolidación de la lista de capturas de portal pendientes (estilo numerado de Lab 2, que usó 01–14).
- **Por qué:** regla dura de misión "Lab 2 no debe romperse" + evidencia de portal solo la puede generar el usuario (sesión web).
- **Resultado — Lab 2 intacto (verificado 19:33–19:35 y 19:54):** `git status --porcelain` en `taller_2_iot/` muestra solo las 6 entradas pre-existentes/legítimas (addendum D5 en bitácora = +5 líneas append-only; guia-vm-azure.md y cpp con mtimes 09-09/12:33 PRE-misión; helpers tools/); `py_compile python-vm-01.py` exit 0; `mqtt_explicito.py` no importa nada de Lab 2 ni `azure-iot-device` (imports: stdlib l.21–33 + paho l.38–39; las 2 coincidencias "azure-iot-device" son docstring/comentario); `.env` y `__pycache__` gitignoreados (reglas :2/:3).
- **Resultado — Capturas portal pendientes (el usuario las toma al cerrar sesión en IoT Central):**

| # | Archivo propuesto | Qué debe mostrar | Dónde (portal IoT Central) |
|---|---|---|---|
| 15 | `15-data-explorer-telemetria.png` | Telemetría Temperature/Humidity/Iluminance del device `23z8rpgm6s4` durante corrida paho (19:22–19:52) | Data explorer → dispositivo → Telemetría |
| 16 | `16-smoke-explicito-data-explorer.png` | Gráfica del smoke-run de las 13:23 (Etapa 2) conviviendo con la del SDK | Data explorer → dispositivo → Telemetría (rango 13:00–13:30) |
| 17 | `17-gemelo-propiedades-deseadas.png` | Gemelo con `Set_temp_hvac`, `ID_salon`, `Estado_semaforo_LED` (reported vs desired) | Management pane → Gemelo (JSON) |
| 18 | `18-metodo-directo-respuesta.png` | Invocación de método directo desde Central y respuesta del cliente explícito (log local `method 200`) | Management pane → Métodos directos |
| 19 | `19-alerta-t27-email.png` | Regla `Temperature > 27` → Email disparada (addendum Lab 2) con correo recibido | Rules + bandeja de destino |
| 20 | `20-c2d-recibido.png` | Mensaje cloud-to-device desde Central recibido en `devices/23z8rpgm6s4/messages/devicebound/` | Message to device + log local |
| 21 | `21-estado-device-online.png` | Estado "en línea" del device con última conexión durante Run A/C/D | Administración del dispositivo |

## [2026-09-13 19:12 UTC] [VOID — APP EQUIVOCADA] Re-mediciones en la VM — evidencia `evidencias/2026-09-13/`
> **VOID (14:52):** esta entrada se corrió contra la app equivocada. El `.env` de la VM traía credenciales de la app compartida del curso (`python-vm-01`, id scope `0ne00FE0977`, hub `iotc-631aa800-...` — app que el docstring del script llamaba "laboratorio1caro"), NO la app del usuario `clima-salones-app-jose` (devices `23z8rpgm6s4`/`esp32-wokwi-01`, id scope `0ne0128547D`, hub `iotc-47a415d3-...`). Los logs de `evidencias/2026-09-13/` se conservan como evidencia secundaria del protocolo (los mecanismos MQTT/DPS/QoS medidos son los mismos en cualquier hub de Central), pero **ningún número de esta entrada pertenece a la app entregable**. Ver entrada corregida de 2026-09-13 más abajo.
- **Qué:** la VM de Azure había quedado APAGADA/SUSPENDIDA durante las corridas del 09-11; al encenderla se verificó `System clock synchronized: yes` y offset NTP ~0.0 s. Se re-ejecutó la batería formal completa del cliente explícito oficial (`mqtt_explicito.py`) con el reloj en hora, en una venv nueva `venv-mqtt3` con paho-mqtt 2.x (la venv vieja tenía paho 1.6.1 y el script oficial exige CallbackAPIVersion de paho>=2).
- **Resultados (todos con marca de tiempo UTC real, payload 60 B por mensaje, interval 10 s):**
  - `run-qos1` — 6 publicados / 6 PUBACK, 1 conexión, **RTT avg 249.3 ms (222.2–359.0)**; coherente con el rango del 09-11 (169–268 ms): la latencia no cambió, solo eran falsas las horas del log anterior.
  - `run-qos0` — 3/3, "RTT" 0.4–1.4 ms = ack LOCAL (sin confirmación del broker, como espera QoS 0).
  - `run-qos2` — 3/3 acks, RTT avg 270.1 ms. Con `--debug-packets` (`run-qos2-packets.log`): **6 PUBACK, CERO PUBREC/PUBREL/PUBCOMP en el alambre** → confirma el hallazgo: IoT Hub degrada QoS 2 a QoS 1.
  - `run-corte` (`--cut-at 25`) — 6 publicados / 6 acks, **2 conexiones**: corte simulado a los 25 s → reconexión con backoff de paho, ningún mensaje perdido.
- **Protocolo DPS visible (script auxiliar `mqtt-explicit-01.py` en la VM, log `mqtt_test_dps_q1.log`):** registro manual por MQTT puro: CONNACK ok → `PUT $dps/registrations/PUT/iotdps-register/?$rid=1` → `202 status=assigning (retry-after=3)` → `GET ...iotdps-get-operationstatus` → `200 status=assigned, assignedHub=iotc-631aa800-...`. Detalle útil: `global.azure-devices-provisioning.net` resuelve; el subdominio `{id-scope}.global...` devolvía NXDOMAIN desde la VM — tal como hace el SDK real.
- **Lab 2 intacto:** `python-vm-01.py` (SDK) reiniciado vía `start.sh` y publicando de nuevo (últimos envíos 19:12:41/19:12:52 UTC). La regla de no tocar Lab 2 se cumplió: los re-runs usaron `venv-mqtt3` separada; la venv del SDK quedó con su paho 1.6.1.
- **Pendiente del usuario (igual que el 09-11):** capturas 15–21 del portal (Data explorer, gemelo, métodos, C2D, estado online). El corte de hoy a las 19:10–19:11 UTC y las corridas 19:07–19:11 UTC ya deberían verse en Data explorer del device.

## [2026-09-13 19:56 UTC] Corrección mayor: la VM estaba apuntando a la app equivocada — re-medición completa contra `clima-salones-app-jose` (`evidencias/miapp-2026-09-13/`)
- **Qué pasó:** el `.env` de la VM tenía credenciales de una app distinta de la del usuario (device `python-vm-01`, id scope `0ne00FE0977`, hub `iotc-631aa800-...`; el docstring del script la llamaba "laboratorio1caro"). La app REAL del usuario es `clima-salones-app-jose.azureiotcentral.com` (devices `23z8rpgm6s4` y `esp32-wokwi-01`, id scope `0ne0128547D`, hub `iotc-47a415d3-3f15-4cf1-918c-4be1461915f3.azure-devices.net` — el mismo hub ya documentado en Etapa 1 y en Lab 2). El runner de la sesión anterior había encendido el SDK con esas credenciales ajenas.
- **Fix:** se respaldó el `.env` de la VM como `.env.bak-otroapp` y se reconstruyó concatenando el `.env` legítimo de Lab 2 (`taller_2_iot/.env`, variables `AZURE_*`) + el de Lab 3 (`taller_3_iot/.env`, `IOT_HUB_HOSTNAME/DEVICE_ID/DEVICE_PRIMARY_KEY`), subidos vía SSH (los secretos nunca se imprimieron). Se limpiaron también los `\r` de Windows del `.env`.
- **Verificación de identidad (no negociable):** DPS respondió `status=assigned` con `assignedHub=iotc-47a415d3-...` y `device=23z8rpgm6s4` — coincide 1:1 con la asignación documentada el 09-11 (línea de Etapa 1 arriba). Todo lo que sigue es contra la app correcta.
- **Nuevas mediciones formales (reloj NTP sincronizado, 19:50–19:55 UTC, payload 60 B, interval 10 s):**
  - **SDK Lab 2** (`python-vm-01.py`, venv original): reconectó por DPS al hub correcto y publica cada 10 s (`run.log` 19:50 y 19:54 UTC) → device `23z8rpgm6s4` otra vez en línea en Central.
  - **DPS 100 % a mano** (`dps-manual-y-telemetria.log`): CONNACK → `PUT $dps/registrations/PUT/iotdps-register/?$rid=1` → `202 assigning (retry-after=3)` → `GET ...iotdps-get-operationstatus` → `200 assigned`; luego 3/3 publicaciones con PUBACK. Dos lecciones descubiertas ejecutando: (1) el subdominio `{id-scope}.global.azure-devices-provisioning.net` NO resuelve (NXDOMAIN) — hay que conectar a `global.azure-devices-provisioning.net` a secas y llevar el id-scope en el username, igual que hace el SDK; (2) tras un `assigning` hay que hacer GET-operationstatus, NO re-PUT (re-PUTs repetidos generan `409 errorCode 409203 Precondition failed` — evidencia cruda preservada en la sesión anterior).
  - **`qos1`** — 6/6 PUBACK, 1 conexión, RTT avg **230.6 ms** (206.1–333.9).
  - **`qos0`** — 3/3, "RTT" 0.3–0.4 ms = ack LOCAL (QoS 0 no tiene confirmación de red, como corresponde).
  - **`qos2`** (+`--debug-packets`) — 3/3 acks, RTT avg 252.8 ms; en el alambre: **0 PUBREC/PUBREL/PUBCOMP, 6 PUBACK** → confirmado también en la app del usuario: IoT Central degrada QoS 2 a QoS 1.
  - **`corte`** (`--cut-at 25`) — 6/6 acks con **2 conexiones**: el socket se corta a los 25 s, paho reconecta con backoff y no se pierde ningún mensaje.
- **Coherencia con lo medido contra la otra app (entrada VOID):** los valores son del mismo orden (RTT ~230–250 ms; QoS 2 degradado; reconexión sin pérdidas) — esperado, porque el mecanismo del hub de Central es idéntico; la diferencia era solo de identidad/timestamps, no de protocolo.
- **Pendiente del usuario (capturas 15–21, ahora sí en `clima-salones-app-jose`):** Data explorer del device `23z8rpgm6s4` mostrando la telemetría de 19:50–19:55 UTC (mezcla de SDK + explícito), estado en línea, gemelo, método directo, C2D y alerta. Wokwi (`esp32-wokwi-01`) se levanta desde el navegador cuando el usuario quiera; no bloquea nada de arriba.

## [2026-09-13 22:02 UTC] Sesión interactiva portal↔VM: métodos directos y gemelo capturados por el cliente explícito
- **Gemelo (twin) por MQTT puro:** al conectar, `mqtt_explicito.py` emitió su `GET $iothub/twin/GET/$/?$rid=1` y recibió `$iothub/twin/res/200` con `{desired:{}, reported:{ID_salon:"A-301", Set_temp_hvac:22.0, Estado_semaforo_LED:"Amarillo", firmwareVersion, ipAddress}}` + `res/204` para el get con `$version=44482` (gemelo sin desired al inicio). Evidencia: `evidencias/miapp-2026-09-13/gemelo-get-inicial.{log,jsonl}` → cubre la captura 17 desde el lado dispositivo (el usuario complementa con la pestaña Properties del portal).
- **Métodos directos (captura 18):** cuatro invocaciones `force_reading` hechas por la usuaria desde IoT Central (portal 16:34–16:40 hora Colombia) llegaron al cliente explícito como `$iothub/methods/POST/force_reading/?$rid=N` y fueron respondidas `200` por el propio cliente (handler manual) — registro en `evidencias/miapp-2026-09-13/metodo-c2d.{log,jsonl}`. Nota: en la corrida de hoy el payload del portal llegó vacío (`bytes: 4` = `{}`); el método igualmente se ejecutó y respondió, cumpliendo el checkpoint "suscribirse al topic de comandos para ver un comando".
- **Operativa:** durante la sesión se detuvo el SDK (misma identidad device = colisión de conexión); al terminar, `python-vm-01.py` fue reiniciado y publica de nuevo cada 10 s (22:02 UTC ok). La UI nueva del portal ya no expone "Send message to device" en el menú del device (solo Send a command/Block/Attach/Rename…): la evidencia C2D (captura 20) se hará vía API REST `POST /api/devices/23z8rpgm6s4/sendMessage` con token "API: Device/Execute" (pendiente, con la usuaria).
- **Lección para el informe:** `Set_temp_hvac` es propiedad escribible (va por gemelo/Properties), NO comando — el popup de métodos del portal la listaba aparte y confundió; los comandos reales de la plantilla son `force_reading` y `Encender_hvac` (payload `{"state":true}`).

## [2026-09-13 22:26 UTC] Hallazgo: C2D ya NO existe en IoT Central moderno (intentado vía token API "lab3-c2d")
- **Qué:** para la captura 20 (mensaje cloud-to-device) se creó token API con rol App Operator. GET `/api/devices/23z8rpgm6s4?api-version=1.0` → **200** (token válido, app y device confirmados por API — evidencia adicional de identidad). POST `/api/devices/23z8rpgm6s4/sendMessage?api-version=1.0` → **404 `NotFound: API not found`** (Request ID 25ookfjf).
- **Verificación:** la referencia REST oficial de IoT Central (learn.microsoft.com/rest/api/iotcentral, groups: Devices, Device Groups, Jobs, Query…) no incluye ningún endpoint de mensaje C2D; la UI nueva tampoco lo expone (el menú del device solo ofrece "Send a command", no "Send message"). Es un retiro funcional de plataforma, no un error del laboratorio.
- **Qué queda como evidencia downlink:** (a) suscripciones del cliente explícito CONCEDIDAS vía SUBACK — incluido `devices/23z8rpgm6s4/messages/devicebound/#` (`c2d-run.log` 22:21Z, SUBACK ×4 QoS 1); (b) el downlink real probado end-to-end es por la otra vía soportada: **métodos directos** (4× `force_reading` POST → respuesta 200 del cliente sin SDK) y **gemelo** (GET 200 con desired/reported). La escritura desired de `Set_temp_hvac` desde Properties no alcanzó a caer en la ventana del listener (expiró 22:25:30Z); si se desea, se relanza listener 5 min y se edita la propiedad — no es bloqueante: la suscripción twin PATCH desired ya está concedida y demostrada funcional en `metodo-c2d.log` (PATCH recibido en sesión anterior del 09-11).
- **Seguridad:** token `lab3-c2d` usado solo en headers; no quedó en repo/logs; el usuario debe eliminarlo en App management → API tokens (hecho al cerrar).
- **SDK restaurado** a las 22:26:26Z publicando `Temperature/Humidity/Iluminance` (mayúsculas) cada 10 s → capturas 15/21/17 disponibles en vivo.
